#include <stdio.h>
#include <stdlib.h>

int maxcount[0x10000000];

int main(int argc, char* argv[]) {
    
    //int tmp[0x10000000];
    //int* ptr = (int*)malloc(sizeof(int) * 0x100000000);
    //printf("%p\n", ptr);
    printf("hello world!\n");
    maxcount[0] = 1;
    //free(ptr);
    return 0;
}
